operationId: TravelNotesController_list
summary: 여행기 목록 조회
description: 조회 대상 사용자의 여행기 목록 조회
responses:
  '200':
    description: ''
    content:
      application/json:
        schema:
          type: object
          properties:
            startDate:
              type: string
              description: 여행 시작일
              example: '2024-06-14'
            endDate:
              type: string
              description: 여행 종료일
              example: '2024-06-17'
            title:
              type: string
              description: 제목
              example: '신나는 서울 여행'
            review:
              type: string
              description: 여행기
              example: '서울 여행 재밌다.'
            cityId:
              type: string
              description: 도시ID
              example: 1
            cityName:
              type: string
              description: 도시명
              example: '서울'
            countryCode:
              type: string
              description: 국가 코드
              example: 'KR'
            mainImageIndex:
              type: number
              description: 메인 이미지 인덱스
              example: 1
  '401':
    description: ''
    content:
      application/json:
        schema:
          example:
            status: 401
            timestamp: '2024-03-30T22:00:00.000Z'
            errors:
              - message: 액세스 토큰이 유효하지 않습니다.

tags:
  - 여행기
security:
  - bearerAuth: []
